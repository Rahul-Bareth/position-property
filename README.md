# CSS Position

A beginner-friendly CSS practice project that explores the `position` property (`static`, `relative`, `absolute`, `fixed`, and `sticky`) to understand how elements are positioned and displayed within a webpage.